
public class Frame {
    public String frameF(int y, int x) {
	String frame = "";
	int row;
	for(row=0;row<y;row++) {
	    int col;
	    if(row == 0 || row == y-1) {
		for(col=0;col<x;col++) {
		    frame = frame + "*";
		}
	    } else {
		for(col=0;col<x;col++) {
		    if(col
		}

	    }
	}
    }

}
