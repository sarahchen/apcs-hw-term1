
public class Greeter4 {
    // INSTANCE VARIABLES
    //Instance variables are almost always private
    private  String greeting = new String("Hello World!");

    // CONSTRUCTORS
    /* Note: - the name is the same as the class name
             - there is no return type (nothing b/w public and name)
	     - it's ALWAYS public
	      
	     a constructor is run automatically when you instantiate the class.
	     that is, if you have Greeter g = new Greeter("Hello")
	     then Java will automatically call this the constructor sending Hello
	     in as parameter g. It will then set the instance variable
	     greeting to what g refers to, which is hello
    */
    
    

    // REGULAR METHODS
    public void setGreeting(String s) {
	greeting = s;
    }

    // String is the "return type" that is, the type of the data that we
    // will send back using the return statement.
    // void is a special return type which means we won't return anything of
    // value
    public String getGreeting() {
	return greeting;
    }

    // Generally, methods don't print, they return.
    public String greet() {
	//	String greeting = new String("Sup!");
	//      System.out.println(greeting);
	return greeting;
    }
    
    public void ungreet() {
	System.out.println("I'm out.");
    }

}
